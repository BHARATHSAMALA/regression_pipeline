This repo contains regression pipeline code with the following:
> Uses used car data
> uses model trainer class to train and save models
> uses grid search for hyperparameter tuning
> displays the results in pretty table

Todo:
> outlier treatment
> Lasso
> lgbm
> xgboost
> ridge

